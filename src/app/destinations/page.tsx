import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { destinations } from "@/lib/demo-data";

export default function DestinationsPage() { return <main className="min-h-screen bg-mist pb-24"><section className="bg-sand py-16"><div className="container-page"><p className="eyebrow">The map is only the beginning</p><h1 className="display mt-2 max-w-2xl text-5xl font-bold leading-tight sm:text-7xl">Places that stay with you.</h1><p className="mt-5 max-w-lg text-sm leading-7 text-slate-500">Explore Kenya's iconic landscapes, hidden corners and the people who know them best.</p></div></section><div className="container-page grid gap-5 py-12 sm:grid-cols-2 lg:grid-cols-4">{destinations.map(destination => <Link href={`/trips?destination=${encodeURIComponent(destination.name)}`} key={destination.name} className="group relative h-[330px] overflow-hidden rounded-3xl"><Image src={destination.image} alt={destination.name} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width: 640px) 90vw, 25vw" /><div className="absolute inset-0 bg-gradient-to-t from-ink/80 via-ink/5 to-transparent" /><div className="absolute bottom-5 left-5 right-5 text-white"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-black">{destination.name}</h2><p className="mt-1 text-xs text-white/70">{destination.count}</p></div><span className="grid h-9 w-9 place-items-center rounded-full bg-white/15"><ArrowUpRight size={17} /></span></div><p className="mt-3 text-xs leading-5 text-white/75">{destination.description}</p></div></Link>)}</div></main>; }
