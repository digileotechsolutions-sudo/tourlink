import { SlidersHorizontal } from "lucide-react";
import { vehicles } from "@/lib/demo-data";
import { VehicleCardView } from "@/components/ui";
import { SearchPanel } from "@/components/search-panel";

export default async function VehiclesPage({ searchParams }: { searchParams: Promise<{ search?: string }> }) { const params = await searchParams; const query = (params.search || "").toLowerCase(); const results = query ? vehicles.filter(v => `${v.name} ${v.type} ${v.location}`.toLowerCase().includes(query)) : vehicles; return <main className="min-h-screen bg-mist pb-24"><section className="bg-lagoon py-12 text-white sm:py-16"><div className="container-page"><p className="eyebrow text-sun">Go your own way</p><h1 className="display mt-2 text-4xl font-bold sm:text-6xl">The right ride is here.</h1><div className="mt-8 max-w-4xl"><SearchPanel compact /></div></div></section><div className="container-page py-10"><div className="mb-7 flex items-center justify-between"><p className="text-sm text-slate-500">{results.length} vehicles ready to go</p><button className="flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2.5 text-xs font-extrabold"><SlidersHorizontal size={15} /> Filters</button></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{results.map(vehicle => <VehicleCardView key={vehicle.id} vehicle={vehicle} />)}</div></div></main>; }
