import { NextResponse } from "next/server";
import { z } from "zod";
import { getCurrentUser } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export async function GET(request: Request, { params }: { params: Promise<{ id: string }> }) { const user = await getCurrentUser(); if (!user || user.role !== "VEHICLE_OWNER") return NextResponse.json({ error: "Vehicle owner access required" }, { status: 403 }); const { id } = await params; const vehicle = await prisma.vehicle.findFirst({ where: { id, ownerId: user.id } }); if (!vehicle) return NextResponse.json({ error: "Vehicle not found." }, { status: 404 }); const dates = await prisma.vehicleAvailability.findMany({ where: { vehicleId: id }, orderBy: { date: "asc" } }); return NextResponse.json({ dates }); }
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const user = await getCurrentUser(); if (!user || user.role !== "VEHICLE_OWNER") return NextResponse.json({ error: "Vehicle owner access required" }, { status: 403 }); const { id } = await params; try { const input = z.object({ date: z.string(), available: z.boolean() }).parse(await request.json()); const vehicle = await prisma.vehicle.findFirst({ where: { id, ownerId: user.id } }); if (!vehicle) return NextResponse.json({ error: "Vehicle not found." }, { status: 404 }); const date = new Date(input.date); const availability = await prisma.vehicleAvailability.upsert({ where: { vehicleId_date: { vehicleId: id, date } }, create: { vehicleId: id, date, available: input.available }, update: { available: input.available } }); return NextResponse.json({ availability }); } catch { return NextResponse.json({ error: "Availability could not be saved." }, { status: 400 }); } }
