import Link from "next/link";
import { ArrowRight, BadgeCheck, CalendarCheck, MessageCircle, ShieldCheck } from "lucide-react";

export function InfoPage({ eyebrow, title, intro, sections }: { eyebrow: string; title: string; intro: string; sections: { title: string; body: string }[] }) {
  return <main className="min-h-screen bg-mist pb-20"><section className="bg-ink py-16 text-white sm:py-24"><div className="container-page"><p className="eyebrow text-sun">{eyebrow}</p><h1 className="display mt-3 max-w-3xl text-5xl font-bold sm:text-7xl">{title}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-white/65">{intro}</p></div></section><section className="container-page grid gap-5 py-10 sm:grid-cols-2">{sections.map((section, index) => <article key={section.title} className="rounded-2xl bg-white p-6 shadow-soft"><span className="grid h-10 w-10 place-items-center rounded-xl bg-sand text-lagoon">{index === 0 ? <ShieldCheck size={19} /> : index === 1 ? <BadgeCheck size={19} /> : index === 2 ? <CalendarCheck size={19} /> : <MessageCircle size={19} />}</span><h2 className="mt-5 text-xl font-black">{section.title}</h2><p className="mt-3 text-sm leading-7 text-slate-500">{section.body}</p></article>)}</section><div className="container-page"><Link href="/trips" className="inline-flex items-center gap-2 rounded-full bg-sun px-5 py-3 text-sm font-extrabold text-ink">Explore trips <ArrowRight size={16} /></Link></div></main>;
}
