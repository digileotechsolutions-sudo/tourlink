"use client";
import { Loader2, Save, UserRound } from "lucide-react";
import { useState } from "react";

export function ProfileForm({ initial }: { initial: { name: string; email: string; phone: string | null; avatarUrl: string | null; bio: string | null; preferredCurrency: string } }) { const [form, setForm] = useState(initial); const [saving, setSaving] = useState(false); const [notice, setNotice] = useState(""); async function save(event: React.FormEvent) { event.preventDefault(); setSaving(true); setNotice(""); const response = await fetch("/api/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); setSaving(false); setNotice(response.ok ? "Profile saved." : data.error || "Could not save profile."); } function upload(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file || !file.type.startsWith("image/") || file.size > 1_500_000) { setNotice("Choose an image under 1.5MB."); return; } const reader = new FileReader(); reader.onload = () => setForm(current => ({ ...current, avatarUrl: String(reader.result) })); reader.readAsDataURL(file); } return <form onSubmit={save} className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><div className="grid h-16 w-16 place-items-center overflow-hidden rounded-full bg-sand text-lagoon">{form.avatarUrl ? <img src={form.avatarUrl} alt="Profile" className="h-full w-full object-cover" /> : <UserRound size={26} />}</div><div><p className="eyebrow">Your identity</p><h2 className="mt-1 text-xl font-black">Profile details</h2><label className="mt-2 inline-block cursor-pointer text-xs font-bold text-lagoon">Upload profile photo<input type="file" accept="image/png,image/jpeg,image/webp" onChange={upload} className="sr-only" /></label></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Full name</span><input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Email</span><input value={form.email} disabled className="w-full rounded-xl border border-slate-100 bg-slate-50 px-4 py-3 text-sm text-slate-400 outline-none" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Phone number</span><input value={form.phone || ""} onChange={e => setForm({ ...form, phone: e.target.value })} placeholder="+254 712 345 678" className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Profile photo URL</span><input value={form.avatarUrl?.startsWith("data:") ? "Uploaded image" : form.avatarUrl || ""} onChange={e => setForm({ ...form, avatarUrl: e.target.value || null })} placeholder="https://..." className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label></div><label className="mt-4 block"><span className="mb-1.5 block text-xs font-bold text-slate-600">A little about you</span><textarea value={form.bio || ""} onChange={e => setForm({ ...form, bio: e.target.value })} rows={4} maxLength={500} className="w-full resize-none rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" placeholder="What kind of journeys do you love?" /></label><div className="mt-5 flex items-center justify-between"><select value={form.preferredCurrency} onChange={e => setForm({ ...form, preferredCurrency: e.target.value })} className="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-xs font-bold"><option>KES</option><option>UGX</option><option>TZS</option><option>RWF</option><option>USD</option></select><div className="flex items-center gap-3">{notice && <span className="text-xs font-bold text-leaf">{notice}</span>}<button disabled={saving} className="flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-xs font-extrabold text-white">{saving ? <Loader2 size={14} className="animate-spin" /> : <Save size={14} />} Save changes</button></div></div></form>; }
