"use client";
import { Bell, Check, Globe2, LockKeyhole, Smartphone } from "lucide-react";
import { useEffect, useState } from "react";
const key = "tourlink_settings";
export function SettingsForm() { const [settings, setSettings] = useState({ push: true, email: true, reminders: true, location: false }); useEffect(() => { const stored = localStorage.getItem(key); if (stored) setSettings(JSON.parse(stored)); }, []); function toggle(name: keyof typeof settings) { const next = { ...settings, [name]: !settings[name] }; setSettings(next); localStorage.setItem(key, JSON.stringify(next)); } return <div className="grid gap-6"><section className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><p className="eyebrow">Your preferences</p><h2 className="mt-1 text-2xl font-black">Settings</h2><p className="mt-2 text-sm text-slate-500">Choose how TourLink keeps you close to your journeys.</p><div className="mt-7 divide-y divide-slate-100">{[{ key: "push" as const, title: "Push notifications", body: "Booking updates and new messages", icon: Bell }, { key: "email" as const, title: "Email updates", body: "Receipts, confirmations and account notices", icon: Smartphone }, { key: "reminders" as const, title: "Trip reminders", body: "A gentle nudge before you set off", icon: Check }, { key: "location" as const, title: "Location-aware discovery", body: "Show experiences near your current area", icon: Globe2 }].map(item => <div key={item.key} className="flex items-center gap-3 py-4"><span className="grid h-9 w-9 place-items-center rounded-xl bg-sand text-lagoon"><item.icon size={17} /></span><div className="flex-1"><p className="text-sm font-extrabold">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.body}</p></div><button type="button" onClick={() => toggle(item.key)} className={`relative h-6 w-11 rounded-full transition ${settings[item.key] ? "bg-lagoon" : "bg-slate-200"}`} aria-label={`Toggle ${item.title}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${settings[item.key] ? "left-6" : "left-1"}`} /></button></div>)}</div></section><section className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><div className="flex gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-sand text-lagoon"><LockKeyhole size={17} /></span><div><h3 className="text-sm font-extrabold">Security</h3><p className="mt-1 text-xs leading-5 text-slate-500">Your account uses secure sessions and your payment details are never stored by TourLink.</p><button className="mt-4 rounded-full border border-slate-200 px-4 py-2.5 text-xs font-extrabold">Change password</button></div></div></section></div>; }
