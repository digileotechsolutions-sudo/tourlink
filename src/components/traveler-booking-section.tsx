import Image from "next/image";
import Link from "next/link";
import { CalendarDays, CarFront, CheckCircle2, Clock3, MapPin, Ticket, XCircle } from "lucide-react";

type TravelerBooking = { id: string; reference: string; status: string; type: string; startDate: Date; endDate: Date; amount: number; currency: string; item: string; destination: string; image: string | null; paymentStatus: string };

export function TravelerBookingSection({ title, eyebrow, bookings, empty }: { title: string; eyebrow: string; bookings: TravelerBooking[]; empty: string }) { return <div className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><div className="flex items-end justify-between"><div><p className="eyebrow">{eyebrow}</p><h2 className="mt-1 text-2xl font-black">{title}</h2></div><Link href="/trips" className="rounded-full bg-sun px-4 py-2.5 text-xs font-extrabold">Explore trips</Link></div>{bookings.length ? <div className="mt-7 grid gap-3">{bookings.map(booking => <article key={booking.id} className="flex gap-4 rounded-xl border border-slate-100 p-4"><div className="relative grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-xl bg-sand text-lagoon">{booking.image ? <Image src={booking.image} alt={booking.item} fill className="object-cover" sizes="80px" /> : booking.type === "VEHICLE" ? <CarFront size={24} /> : <Ticket size={24} />}</div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><div><h3 className="truncate text-sm font-extrabold">{booking.item}</h3><p className="mt-1 text-xs text-slate-500"><MapPin size={13} className="mr-1 inline text-sun" />{booking.destination}</p></div><Status status={booking.status} /></div><p className="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-xs text-slate-500"><span><CalendarDays size={13} className="mr-1 inline" />{booking.startDate.toLocaleDateString("en-KE", { day: "numeric", month: "short", year: "numeric" })} - {booking.endDate.toLocaleDateString("en-KE", { day: "numeric", month: "short", year: "numeric" })}</span><span className="font-bold text-ink">{booking.currency} {booking.amount.toLocaleString("en-KE")}</span></p><p className="mt-2 text-[10px] font-bold text-slate-400">{booking.reference} · payment {booking.paymentStatus.toLowerCase()}</p></div></article>)}</div> : <div className="grid place-items-center py-16 text-center"><Clock3 className="text-sun" /><h3 className="mt-4 font-extrabold">Nothing here yet</h3><p className="mt-2 max-w-sm text-sm text-slate-500">{empty}</p></div>}</div>; }
function Status({ status }: { status: string }) { const active = ["PAID", "CONFIRMED", "IN_PROGRESS"].includes(status); return <span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-[10px] font-black ${active ? "bg-leaf/10 text-leaf" : status === "CANCELLED" ? "bg-red-50 text-red-600" : status === "COMPLETED" ? "bg-lagoon/10 text-lagoon" : "bg-sand text-[#a45913]"}`}>{active ? <CheckCircle2 size={12} /> : status === "CANCELLED" ? <XCircle size={12} /> : <Clock3 size={12} />}{status}</span>; }
