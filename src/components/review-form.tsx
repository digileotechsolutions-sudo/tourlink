"use client";
import { Loader2, Star } from "lucide-react";
import { useState } from "react";

export function ReviewForm({ bookingId }: { bookingId: string }) { const [rating, setRating] = useState(5); const [body, setBody] = useState(""); const [notice, setNotice] = useState(""); const [saving, setSaving] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setSaving(true); const response = await fetch("/api/reviews", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ bookingId, rating, communication: rating, service: rating, quality: rating, value: rating, body }) }); const data = await response.json(); setSaving(false); setNotice(response.ok ? "Thank you for sharing your journey." : data.error); } return <form onSubmit={submit} className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><p className="eyebrow">Give back to the road</p><h2 className="mt-1 text-2xl font-black">How was your journey?</h2><div className="mt-6 flex gap-2">{[1, 2, 3, 4, 5].map(value => <button type="button" key={value} onClick={() => setRating(value)} aria-label={`${value} stars`}><Star size={27} fill={value <= rating ? "#ee8d3b" : "transparent"} color={value <= rating ? "#ee8d3b" : "#cbd5e1"} /></button>)}</div><p className="mt-2 text-xs font-bold text-slate-500">{rating} out of 5</p><textarea required minLength={10} value={body} onChange={e => setBody(e.target.value)} rows={6} placeholder="Tell future travelers what stood out..." className="mt-6 w-full resize-none rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" />{notice && <p className="mt-3 rounded-xl bg-sand p-3 text-xs font-bold text-ink">{notice}</p>}<button disabled={saving} className="mt-5 flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-xs font-extrabold text-white">{saving && <Loader2 size={14} className="animate-spin" />} Publish review</button></form>; }
