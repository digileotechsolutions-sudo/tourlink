"use client";
import { Building2, Loader2, Save, ShieldCheck } from "lucide-react";
import { useState } from "react";
type Profile = { companyName: string; description: string | null; logoUrl: string | null; website: string | null; yearsActive: number };
export function OperatorProfileForm({ initial }: { initial: Profile }) { const [form, setForm] = useState({ ...initial, description: initial.description || "", website: initial.website || "" }); const [notice, setNotice] = useState(""); const [saving, setSaving] = useState(false); function upload(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file || !file.type.startsWith("image/") || file.size > 1_500_000) { setNotice("Choose a logo image under 1.5MB."); return; } const reader = new FileReader(); reader.onload = () => setForm(current => ({ ...current, logoUrl: String(reader.result) })); reader.readAsDataURL(file); } async function save(event: React.FormEvent) { event.preventDefault(); setSaving(true); setNotice(""); const response = await fetch("/api/operator/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); setSaving(false); setNotice(response.ok ? "Company profile saved." : data.error || "Could not save profile."); } return <form onSubmit={save} className="rounded-2xl bg-white p-5 shadow-soft sm:p-7"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><div className="grid h-16 w-16 place-items-center overflow-hidden rounded-2xl bg-sand text-lagoon">{form.logoUrl ? <img src={form.logoUrl} alt="Company logo" className="h-full w-full object-cover" /> : <Building2 size={26} />}</div><div><p className="eyebrow">Your business</p><h2 className="mt-1 text-xl font-black">Company profile</h2><label className="mt-2 inline-block cursor-pointer text-xs font-bold text-lagoon">Upload company logo<input type="file" accept="image/png,image/jpeg,image/webp" onChange={upload} className="sr-only" /></label></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Company name</span><input required value={form.companyName} onChange={event => setForm({ ...form, companyName: event.target.value })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Website</span><input value={form.website} onChange={event => setForm({ ...form, website: event.target.value })} placeholder="https://yourcompany.com" className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Years in business</span><input type="number" min="0" max="100" value={form.yearsActive} onChange={event => setForm({ ...form, yearsActive: Number(event.target.value) })} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label><label><span className="mb-1.5 block text-xs font-bold text-slate-600">Logo URL</span><input value={form.logoUrl?.startsWith("data:") ? "Uploaded logo" : form.logoUrl || ""} onChange={event => setForm({ ...form, logoUrl: event.target.value || null })} placeholder="https://..." className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" /></label></div><label className="mt-4 block"><span className="mb-1.5 block text-xs font-bold text-slate-600">Business description</span><textarea required value={form.description} onChange={event => setForm({ ...form, description: event.target.value })} rows={6} maxLength={1000} className="w-full resize-none rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-lagoon" placeholder="Tell travelers what makes your company special." /></label><div className="mt-5 flex items-center justify-between"><p className="flex items-center gap-2 text-xs text-slate-400"><ShieldCheck size={15} className="text-leaf" /> Verification is managed separately</p><div className="flex items-center gap-3">{notice && <span className="text-xs font-bold text-leaf">{notice}</span>}<button disabled={saving} className="flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-xs font-extrabold text-white">{saving ? <Loader2 size={14} className="animate-spin" /> : <Save size={14} />} Save profile</button></div></div></form>; }
